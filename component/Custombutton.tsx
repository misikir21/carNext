import React from "react";

const Custombutton = () => {
  return (
    <button
      disabled={false}
      type={"button"}
      className={"custom-btn"}
      onClick={() => {}}
    ></button>
  );
};

export default Custombutton;
